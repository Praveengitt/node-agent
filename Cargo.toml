[package]
name = "node-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.37"

reqwest = { version = "0.12", features = ["json", "blocking"]}